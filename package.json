{
  "name": "testrepository",
  "version": "1.0.0",
  "private": true,
  "description": "Hello World TypeScript-Projekt, deployt als GitHub Page",
  "scripts": {
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\"",
    "build": "npm run clean && tsc && node -e \"require('fs').cpSync('public','dist',{recursive:true})\""
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
